# Copas

## Your AI-built app is ready. Time to go live.

Deploy local source to have live URL with `copas up` without leaving your AI Prompt.

## Prompt

```md
First, install Copas CLI with `curl -fsSL https://copas.sh/install | sh`, then run `copas skill --auto`. Deploy this project to Copas: review the repo, ask once before changes, run `copas up`, verify and return live URL.
```

## Install

```bash
curl -fsSL https://copas.sh/install | sh
```
